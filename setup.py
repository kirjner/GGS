#!/usr/bin/env python

from setuptools import find_packages, setup

setup(
    name="ggs",
    version="0.0.1",
    description="This project implements Gibbs sampling with Graph-based Smoothing (GGS) on GFP and AAV data ",
    packages=find_packages(),
)
